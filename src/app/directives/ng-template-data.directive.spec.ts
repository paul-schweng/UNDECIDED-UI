import { NgTemplateDataDirective } from './ng-template-data.directive';

/*
describe('NgTemplateDataDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTemplateDataDirective();
    expect(directive).toBeTruthy();
  });
});

 */
